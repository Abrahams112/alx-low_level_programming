#include "main.h"

/**
 * factorial(int n) - print factoril of a given number
 * @n: given number
 */
int factorial(int n);
{
	if (n < 0)
		return (-1);

	if (n <= 1 && >= 0)
		return (n * factorial(n - 1));
}
